# Copyright AGNTCY Contributors (https://github.com/agntcy)
# SPDX-License-Identifier: Apache-2.0

# Default values for slim-channel-manager.

# This will set the replicaset count more information can be found here: https://kubernetes.io/docs/concepts/workloads/controllers/replicaset/
replicaCount: 1

# This sets the container image more information can be found here: https://kubernetes.io/docs/concepts/containers/images/
image:
  repository: "ghcr.io/agntcy/slim/channel-manager"
  # This sets the pull policy for images.
  pullPolicy: IfNotPresent
  # Overrides the image tag whose default is the chart appVersion.
  tag: ""

# This is for the secretes for pulling an image from a private repository more information can be found here: https://kubernetes.io/docs/tasks/configure-pod-container/pull-image-private-registry/
imagePullSecrets: []
# This is to override the chart name.
nameOverride: ""
fullnameOverride: "slim-channel-manager"

# This is for overriding the default config below.
overrideConfig:

config:
  channel-manager:
    # Client configuration for connecting to the SLIM data-plane node.
    # Supports gRPC (http://, https://, unix://) and WebSocket (ws://, wss://) transports.
    slim-connection:
      endpoint: "http://slim:46357"
      tls:
        insecure: true

    # gRPC server configuration for the channel management API.
    # Supports gRPC (http://, https://, unix://) and WebSocket (ws://, wss://) transports.
    api-server:
      endpoint: "0.0.0.0:{{ .Values.service.port }}"
      tls:
        insecure: true

    # Local name for the channel manager in SLIM (org/namespace/app format)
    local-name: ""

    # Authentication for the SLIM app identity.
    auth:
      type: shared_secret
      # id: "custom/identity/id"       # Optional, defaults to local-name
      secret: ""  # Required
    # For SPIRE authentication:
    # auth:
    #   type: spire
    #   socket_path: "unix:/tmp/spire-agent/public/api.sock"
    #   target_spiffe_id: ""
    #   jwt_audiences: ["slim"]
    #   trust_domains: []

    # Channels to create on startup
    # This requires that all the participants are already up and running
    # in reachable in the slim network. If not, the channel creation will fail.
    channels: []
    # - name: "org/namespace/channel"
    #   participants:
    #     - "org/namespace/app1"
    #     - "org/namespace/app2"
    #   mls-enabled: true

# This section builds out the service account more information can be found here: https://kubernetes.io/docs/concepts/security/service-accounts/
serviceAccount:
  # Specifies whether a service account should be created
  create: true
  # Automatically mount a ServiceAccount's API credentials?
  automount: true
  # Annotations to add to the service account
  annotations: {}
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template
  name: ""

# This is for setting Kubernetes Annotations to a Pod.
# For more information checkout: https://kubernetes.io/docs/concepts/overview/working-with-objects/annotations/
podAnnotations: {}
# This is for setting Kubernetes Labels to a Pod.
# For more information checkout: https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/
podLabels: {}

podSecurityContext:
  {}
  # fsGroup: 2000

securityContext:
  {}
  # capabilities:
  #   drop:
  #   - ALL
  # readOnlyRootFilesystem: true
  # runAsNonRoot: true
  # runAsUser: 1000

# This is for setting up a service more information can be found here: https://kubernetes.io/docs/concepts/services-networking/service/
service:
  # This sets the service type more information can be found here: https://kubernetes.io/docs/concepts/services-networking/service/#publishing-services-service-types
  type: ClusterIP
  port: 10356

# This block is for setting up the ingress for more information can be found here: https://kubernetes.io/docs/concepts/services-networking/ingress/
ingress:
  enabled: false
  className: ""
  annotations:
    nginx.ingress.kubernetes.io/backend-protocol: "GRPC"
    # kubernetes.io/ingress.class: nginx
    # kubernetes.io/tls-acme: "true"
  hosts:
    - host: chart-example.local
      paths:
        - path: /
          pathType: ImplementationSpecific
  tls: []
  #  - secretName: chart-example-tls
  #    hosts:
  #      - chart-example.local

resources:
  {}
  # We usually recommend not to specify default resources and to leave this as a conscious
  # choice for the user. This also increases chances charts run on environments with little
  # resources, such as Minikube. If you do want to specify resources, uncomment the following
  # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
  # limits:
  #   cpu: 100m
  #   memory: 128Mi
  # requests:
  #   cpu: 100m
  #   memory: 128Mi

extraEnv: []
  # Example:
  # - name: HTTP_PROXY
  #   value: "http://proxy:8080"
  # - name: HTTPS_PROXY
  #   value: "http://proxy:8080"
  # - name: SECRET_VAR
  #   valueFrom:
  #     secretKeyRef:
  #       name: my-secret
  #       key: secret-key

# Additional volumes on the output Deployment definition.
extraVolumes: []
# - name: foo
#   secret:
#     secretName: mysecret
#     optional: false

# Additional volumeMounts on the output Deployment definition.
extraVolumeMounts: []
# - name: foo
#   mountPath: "/etc/foo"
#   readOnly: true

# Persistent storage for channel-manager session state.
# When enabled, sessions survive pod restarts via an encrypted SQLite file.
persistence:
  enabled: false

  # Path inside the container where the SQLite database is stored.
  # path.value is used for the Kubernetes volume mountPath and (by default) in the app config.
  # path.envVar: if set, the app config references ${env:VAR_NAME} instead of the literal value;
  #   you must supply that env var (e.g. via extraEnv) resolving to the same directory.
  path:
    value: /var/lib/slim/channel-manager
    envVar: ""

  # Encryption passphrase — configure exactly one option (unless insecure: true).
  encryptionPassphrase:
    # Option 1: plain value written directly into the config (insecure, for testing only)
    value: ""
    # Option 2: name of an env var already present in the container (supplied via extraEnv)
    #   → chart puts ${env:VAR_NAME} in the app config
    envVar: ""
    # Option 3: Kubernetes Secret — chart injects it as env var CM_PASSPHRASE
    #   → chart puts ${env:CM_PASSPHRASE} in the app config
    secretRef:
      name: ""
      key: "encryption-passphrase"

  # Set true to skip encryption entirely (dev/test only — overrides encryptionPassphrase)
  insecure: false

  # When false (recommended), sessions persist across restarts.
  # When true, sessions are fully deleted from SLIM on shutdown (original behaviour).
  deleteSessionsOnShutdown: false

  # PVC settings (ignored when existingClaim is set)
  size: 1Gi
  storageClass: ""
  accessModes:
    - ReadWriteOnce
  # Provide an existing PVC name to skip PVC creation
  existingClaim: ""

nodeSelector: {}

tolerations: []

affinity: {}
